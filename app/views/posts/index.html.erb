<h1>This is the index page</h1>
<% @posts.each do |post| %>
	<h2><%= link_to post.title, post %></h2>
	<p><%= post.content %></p>
	<p>Published <%= time_ago_in_words(post.created_at) %>ago, by <%= post.user.username %></p>
	<hr>
	<% end %>
	